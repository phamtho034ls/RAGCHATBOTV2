# ── Core Framework ──────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
PyYAML>=6.0
pydantic>=2.0
python-multipart>=0.0.9   # UploadFile / form (document_router_v2)

# ── Document Processing ────────────────────────────────
python-docx>=1.1
pywin32>=306

# ── LLM ────────────────────────────────────────────────
openai>=1.30
httpx>=0.27

# ── Embeddings & Reranking ─────────────────────────────
# Embedding: SentenceTransformer (pipeline/embedding.py)
# Rerank: FlagReranker ưu tiên (reranker.py); fallback CrossEncoder (sentence-transformers)
sentence-transformers>=3.0
FlagEmbedding>=1.2.0
torch>=2.2
transformers>=4.40
numpy<2

# ── PostgreSQL (async) ─────────────────────────────────
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
tqdm>=4.66

# ── Qdrant ─────────────────────────────────────────────
qdrant-client>=1.9

# ── Redis ──────────────────────────────────────────────
redis[hiredis]>=5.0

# ── Web Search (Ninh Bình info) ─────────────────────────
wikipedia>=1.4.0

# ── Dev / tests (make test, pytest backend/tests/) ─────
pytest>=8.0
requests>=2.31
